# Chief Agent

This is a simple agent named Chief.

## Getting Started

1.  **Navigate to the agent's directory:**
    ```bash
    cd python/agents/chief
    ```

2.  **Install dependencies:**
    ```bash
    uv pip install -e .
    ```

3.  **Run the agent:**
    ```bash
    uv run python -m chief.agent
    ```
