# Environment variables for Agent Builder Pro

# Google Cloud Project Configuration
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Staging bucket for agent deployment
# This bucket is used to stage artifacts when deploying agents to Vertex AI
STAGING_BUCKET=gs://your-staging-bucket

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Default model for generated agents
# Options: gemini-2.5-flash, gemini-2.5-pro
DEFAULT_MODEL=gemini-2.5-pro

# Optional: Session management (in_memory or firestore)
SESSION_MANAGEMENT=in_memory
