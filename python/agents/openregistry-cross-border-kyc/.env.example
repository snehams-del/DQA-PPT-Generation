# Gemini API key (needed to call the model). Get one at https://aistudio.google.com/app/apikey.
GOOGLE_API_KEY=

# Override the OpenRegistry MCP endpoint here if you want to point at a
# self-hosted instance. Defaults to the public hosted server.
OPENREGISTRY_MCP_URL=https://openregistry.sophymarine.com/mcp
